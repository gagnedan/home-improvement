<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if user_signed_in? %>
        <%= link_to 'Home Improvement', projects_path, class: "navbar-brand"%>
      <% else %>
        <span class="navbar-brand" href="/">Home Improvement</span>
      <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
			<ul class="nav navbar-nav">
				<% if user_signed_in? %>
      		<li>
              <p class="navbar-btn push-10-l">
                <%= link_to 'New Project', new_project_path, class: "btn btn-success  btn-sm"%>
              </p>
          </li>
      	<% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <% if user_signed_in? %>
            <p class="navbar-btn">
            <%= button_to "Sign out", destroy_user_session_path, method: "delete", class:"btn  btn-primary btn-sm" %>
            </p>
          <% end%>
        </li>
      </ul>
     </div>
  </div>
</nav>